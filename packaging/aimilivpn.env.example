# AimiliVPN native environment file.
# Installers copy this to /etc/default/aimilivpn when needed.

VPNGATE_DATA_DIR=/opt/aimilivpn/vpngate_data
LOCAL_PROXY_HOST=127.0.0.1
LOCAL_PROXY_PORT=7928
UI_HOST=::
UI_PORT=8787
OPENVPN_CMD=openvpn
